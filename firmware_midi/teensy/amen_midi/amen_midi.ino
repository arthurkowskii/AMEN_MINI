// AMEN MIDI — Teensy sketch (minimal entry marker; setup/loop live in
// teensy_port.cpp. The build script symlinks the portable core sources as
// core_*.cpp and passes -I to the engine include root.)
